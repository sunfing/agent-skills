interface:
  display_name: "GPT Image"
  short_description: "Generate and edit images with GPT Image"
  default_prompt: "Use $gpt-image to generate or edit an image through my configured OpenAI-compatible Image API."

policy:
  allow_implicit_invocation: true
